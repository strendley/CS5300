var base = '/#/'
export const HOME = `${base}home`;
export const TITLE = `${base}title`;
export const AUTHOR = `${base}author`;
export const YEARS = `${base}years`;
export const ADD_BOOK = `${base}add_book`;
export const PLAYGROUND = `${base}playground`